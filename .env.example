# --- TTS Model Configuration ---
# Specify the Hugging Face model ID for the TTS model.
# Examples:
# parler-tts/parler-tts-mini-v1.1
# nari-labs/dia
MODEL_ID=parler-tts/parler-tts-mini-v1.1

# --- Flask Server Configuration ---
# Host address for the server
FLASK_HOST=127.0.0.1
# Port for the server
FLASK_PORT=3003

# --- API Configuration ---
# Maximum allowed characters for the input text
MAX_TEXT_LENGTH=1000
# Default speaker description if none is provided in the API request.
# This is primarily used by models like Parler-TTS.
DEFAULT_DESCRIPTION="A female speaker delivers a slightly expressive and animated speech. The recording is of very high quality."

# --- Optional: Torch Device ---
# Force a specific device (e.g., "cuda", "mps", "cpu").
# If commented out or empty, the script will attempt auto-detection.
# TORCH_DEVICE=
